

// /usr/local/bin/live-server (insert at top)
// rename tmux window to " live-server "
const execSync = require('child_process').execSync;
const output = execSync('tmux rename-window " live-server "', { encoding: 'utf-8' });

